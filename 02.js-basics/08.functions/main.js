
// 1. Make a function named logger that will console.log the argument you passed to it.

function logger (info) {
    console.log(info);
}

logger("Print this out to the console please.");

// 2. Make a function called add that adds two numbers together.

function add (num1, num2) {
    return num1 + num2;
}

console.log(add(14,21));


// 3. Make a function called multiply that multiplies two numbers together.

function multiply (num1, num2) {
    return num1 * num2;
}

console.log(multiply(3,12));
